# WebTool - 远程设备管理工具

## 📁 项目结构

```
webtool/
├── app/                          # 应用主目录
│   ├── __init__.py              # Flask应用初始化
│   ├── config.py                # 配置文件
│   └── modules/                 # 功能模块目录
│       ├── __init__.py
│       ├── home/                # 首页模块
│       │   ├── __init__.py
│       │   ├── routes.py        # 路由定义
│       │   ├── templates/       # 模板文件
│       │   │   └── index.html
│       │   └── static/          # 静态资源
│       │
│       ├── remote_access/       # 远程访问模块
│       │   ├── __init__.py
│       │   ├── routes.py        # 路由定义
│       │   ├── services.py      # SSH服务（业务逻辑）
│       │   ├── templates/       # 模板文件
│       │   │   └── remote_access.html
│       │   └── static/          # 静态资源
│       │       ├── remote_access.css
│       │       └── remote_access.js
│       │
│       ├── quick_config/        # 快速配置模块
│       │   ├── __init__.py
│       │   ├── routes.py        # 路由定义
│       │   ├── templates/       # 模板文件
│       │   │   └── quick_config.html
│       │   └── static/          # 静态资源
│       │       └── quick_config.css
│       │
│       └── test/                # 测试模块
│           ├── __init__.py
│           ├── routes.py        # 路由定义
│           ├── templates/       # 模板文件
│           │   └── test.html
│           └── static/          # 静态资源
│
├── templates/                   # 旧版模板（保留）
├── static/                      # 旧版静态文件（保留）
├── run.py                       # 应用入口文件
├── requirements.txt             # 项目依赖
└── hello.py                     # 旧版入口文件（保留）
```

## 🎯 架构设计

### 模块化设计原则

1. **独立性**：每个功能模块独立存在，拥有自己的路由、模板和静态文件
2. **可扩展性**：新增功能只需在 `app/modules/` 下创建新模块
3. **清晰性**：代码结构清晰，便于维护和协作开发

### 核心组件

#### 1. Flask应用工厂模式 (`app/__init__.py`)
- 使用工厂函数 `create_app()` 创建应用实例
- 统一注册所有模块的蓝图
- 集中管理配置

#### 2. 配置管理 (`app/config.py`)
- 集中管理所有配置项
- 支持不同环境的配置切换

#### 3. 模块蓝图 (Blueprint)
- 每个模块使用独立的蓝图
- 路由、模板和静态文件相互隔离
- 便于模块的插拔和重用

## 🚀 快速开始

### 安装依赖

```bash
pip install -r requirements.txt
```

### 运行应用

```bash
python run.py
```

访问 `http://localhost:5000` 即可使用

## 📦 添加新模块

### 步骤1：创建模块目录结构

```bash
mkdir app/modules/new_module
mkdir app/modules/new_module/templates
mkdir app/modules/new_module/static
```

### 步骤2：创建必要文件

#### `app/modules/new_module/__init__.py`
```python
"""新模块初始化"""
```

#### `app/modules/new_module/routes.py`
```python
"""新模块路由"""
from flask import Blueprint, render_template

# 创建蓝图
new_module_bp = Blueprint(
    'new_module',
    __name__,
    template_folder='templates',
    static_folder='static',
    static_url_path='/new_module/static'
)

@new_module_bp.route('/new_module')
def index():
    """新模块首页"""
    return render_template('new_module/templates/index.html')
```

#### `app/modules/new_module/services.py` (可选)
```python
"""新模块业务逻辑"""

class NewModuleService:
    """业务逻辑类"""
    
    def do_something(self):
        pass
```

### 步骤3：注册蓝图

在 `app/__init__.py` 中添加：

```python
from app.modules.new_module.routes import new_module_bp

def create_app():
    app.register_blueprint(new_module_bp)
    return app
```

## 📝 现有模块说明

### 1. Home模块 (首页)
- **路由**: `/`
- **功能**: 应用首页，导航入口

### 2. Remote Access模块 (远程访问)
- **路由**: `/to_remote`
- **功能**: SSH远程连接，批量执行命令
- **特色**: 支持IP批量解析、预设用户、命令历史

### 3. Quick Config模块 (快速配置)
- **路由**: `/to_quick_config`
- **功能**: 快速配置远程设备
- **特色**: 美化UI、配置模板、进度显示

### 4. Test模块
- **路由**: `/to_test`
- **功能**: 测试功能页面

## 🔧 技术栈

- **后端**: Flask 2.3.3
- **SSH**: Paramiko 3.3.1
- **前端**: HTML5 + CSS3 + JavaScript
- **UI**: 渐变色彩、动画效果、响应式设计

## 📌 注意事项

1. 旧版文件（`hello.py`、`templates/`、`static/`）已保留，确保兼容性
2. 新架构使用蓝图（Blueprint）模式，路由前缀可自定义
3. 静态文件路径需使用模块特定的 `url_for`，如：`url_for('module_name.static', filename='...')`
4. 建议在生产环境使用 WSGI 服务器（如 Gunicorn）

## 🎨 特色功能

- ✨ 模块化架构，易于扩展
- 🎨 现代化UI设计
- 🔐 SSH安全连接
- 📊 实时进度显示
- 💾 预设配置支持
- 📱 响应式布局

## 🔜 未来计划

- [ ] 添加用户认证模块
- [ ] 支持配置文件导入导出
- [ ] 添加操作日志记录
- [ ] 支持更多协议（Telnet、SNMP等）
- [ ] 添加设备管理功能

---

**作者**: WebTool Team  
**版本**: 2.0.0  
**更新日期**: 2025-11-29
